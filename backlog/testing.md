# Testing backlog

Goal: test the ACTUAL TOOL and its integrations (whole-path, real binary). Keep
this STRICTLY separate from the bench. Detection accuracy (precision/recall/F1)
is measured ONLY by `tools/secretbench` — never asserted in `cargo test`. And
the bench must never carry test-framework logic.

## Inventory (2026-05-30)
- ~2,629 test files, ~14,988 `#[test]` fns. scanner alone: **1,234 files / 12,840 tests**. core 466, sources 548, verifier 306, cli 828.
- Bench dir (`tools/secretbench`): ZERO `#[test]`/cargo-test cruft → that direction is clean, no fix needed.

## Separation violations (bench logic living in `cargo test`)
- **T-01 · DONE · accuracy-rate runners are all-or-nothing or eprintln-only** — every named runner now either (a) asserts per-credential-sufficient invariance all-or-nothing (no rate gate) or (b) prints rates via `eprintln!` for information only. Verified 2026-06-10: `multi_secret_runner` gates `violations.is_empty()` (credential-sufficiency), its co-location `%` is eprintln-only; `path_shape_runner` gates `production_misses.is_empty()` (strict-by-default), its `%` is eprintln-only; `whitespace_normalization_runner` (2170/2170) and `line_length_runner` (1101/1101) were rewritten to transform only bytes OUTSIDE the credential span and gate each credential-sufficient positive all-or-nothing; `cve_corpus_runner`/`unicode_confusable_runner`/`readme_claims` carry no rate asserts; `*_recall_*` use per-hit confidence floors / no-hit asserts. Aggregate precision/recall/F1 over a corpus stays bench-only (`benchmarks/bench`). The lone ratio assert in `all_detectors_self_validate::detector_contract_coverage_meets_floor` is a TEST-COVERAGE meta-metric (fraction of detectors with a contract file on disk), not a detection rate — the scanner never runs there.

## Decoration / hand-rolled
- **T-02 · DONE · finding-collection shape-asserts replaced with truth-asserts** — every LIVE `assert!(!<findings/creds/secrets/matches/pairs/hits>.is_empty())` was either strengthened to assert the exact credential value + detector_id + line (+ exit code for CLI) or removed as redundant where an exact assert already followed. Sites: `unit/file_gate.rs::entropy_scanner_happy` (exact value + keyword + line + entropy-above-threshold); 5 in `gaps/scan_filters_grouped.rs` (padding recovery / quoted-value / homebrew ghp / padded-value attribution / elasticsearch base64); 4 in CLI (`e2e_binary.rs` planted-AWS + GH-token-schema, `e2e/detectors_search_aws.rs`, `e2e/scan_output_writes_file.rs`). A live-tree grep for finding-collection `!is_empty()` shape-asserts now returns 0 (the only 2 left live in dormant `contract_pending/`, never compiled). Edited scanner + CLI tests pass (4/4 CLI green).
- **T-03 · DONE · hand-rolled fixtures triaged** — detection-accuracy claims (where the secret IS the point) are driven from Tier-B data files: 904 `tests/contracts/*.toml` (positives across code/dotenv/markdown + adversarial envelopes) walked by `contracts_runner.rs`/`path_shape_runner.rs`, all-or-nothing per rule, no rates. Surviving INLINE `AKIA/ghp_/sk_` literals are INPUTS to tool-behavior tests asserting behavior, not detection: `akia_split_across_chunk_boundary_reassembled` (seam reassembly), `decode_pipeline_layers` (base64 roundtrip + malformed-alphabet rejection), `checksum_github` (golden checksum/CRC32 vectors), `multiline_reassembly` (config defaults + offset/line mapping). No inline fixture computes an aggregate accuracy claim.

## Test-effectiveness gaps
- **T-04 · DONE · detector-load-integrity now runs against the EMBEDDED set** — `every_detector_loads` (on-disk count==file-count) did not catch the dead `discord-bot-token` (DET-01) because it parsed on-disk TOMLs, not the bytes compiled into the binary. CLOSED: `crates/core/tests/gap/every_embedded_detector_parses.rs::every_embedded_detector_parses` parses the EXACT `embedded_detector_tomls()` slice the binary ships and fails — naming each offender — if any embedded detector doesn't parse. It lives in the source crate (`keyhog-core`), so every consumer's `cargo test` and CI run enforces it. Paired with the runtime fail-closed in `load_detectors_embedded_or_fail` (a corrupt embedded set now aborts the scan, not a silent debug line). → MC-16 closed.
- **T-05 · DONE · embedded vs on-disk parse divergence** — CLOSED by `every_embedded_detector_parses::embedded_parseable_count_equals_slice_len`: asserts the parseable embedded count == embedded slice length == `embedded_detector_count()`, so an embed-time drop or a malformed embedded TOML cannot pass as a healthy load.

## Real tests to ADD (tool + integration, NOT detection)
- Every subcommand × default + ≥3 flag combos: snapshot stdout/stderr/exit.
- Every output format (text/json/jsonl/sarif/csv/junit/html) byte-compared to a committed fixture (well-formed is not enough).
- Exit-code contract asserted per entry path: 0 clean / 1 findings / 2 runtime error / 3 file error (DF-04 — and document it).
- `daemon`/`watch` via the real socket; `tui` via vt100 render; `hook` install+run; `--git-staged/--git-diff/--git-history` on a real repo; `--incremental` cache round-trip; baseline create/update/diff round-trip.
- GitHub Action, SARIF upload, pre-commit hook: end-to-end against a recorded backend.
- CLI flag-surface snapshot per subcommand so the 68-flag surface can't grow silently (CLI-08).

## Organize / dedup
- **T-06 · DONE · byte-identical helper consolidated; dormant dir flagged** — the 12 byte-identical 2-arg `make_chunk(text, path)` copies collapse to one shared `support::contracts::test_chunk` (delegates to the canonical 3-arg `make_chunk`). The 4 LIVE consumers (`backend_parity_matrix`, `diagnose_sb_divergence`, `gpu_parity`, `fallback_no_hit_branch_recall`) now `use support::contracts::test_chunk as make_chunk;` with zero call-site churn; now-idle `Chunk`/`ChunkMetadata` imports dropped where they fell unused; scanner test crate recompiles clean. The other 8 copies live in `tests/contract_pending/` — 12 untracked `.rs` files (~330KB), never `mod`-wired so never compiled (their asserts never run) and superseded by the richer TOML contracts (e.g. `detector_contract_github.rs`'s inline `ghp_` positives are a strict subset of `github-classic-pat.toml`). They must NOT be wired (that would duplicate the TOML-driven runner); recommended for removal pending owner confirmation — untracked WIP, not deleted without approval per repo policy. Per-detector accuracy now lives in table-driven TOML, preserving per-rule failure attribution.
