# Open gaps ledger — 2026-06-10

Every gap I currently hold, with evidence (`file:line`), status, the exact recipe
to close it, and the blocker if one exists. Status legend:

- **READY** — format/fix confirmed, can land now; cost noted.
- **BLOCKED** — confirmed but gated on something (named).
- **DECLINED** — deliberately not shipping (soundness-over-reach); names the missing asset.
- **LONG-HORIZON** — genuine capability/ML work, not a quick close.
- **RESOLVED** — closed this session; kept here for traceability.

---

## G1 · aws-bedrock-api-key detector · RESOLVED (2026-06-10)

**Shipped.** `detectors/aws-bedrock-api-key.toml` (critical, anchor
`ABSKQmVkcm9ja0FQSUtleS`, `min_confidence = 0.2`) + contract
`crates/scanner/tests/contracts/aws-bedrock-api-key.toml` (positives,
anchor/length negatives, header+comment evasions). README/banner 900→901.
The 782-file count churn was avoided by doing G9 first (count now derives
from the loader). Proven end-to-end: the built binary reports
`detector_id == "aws-bedrock-api-key"` / `severity critical` on a planted key
(`scan_finds_planted_bedrock_key_and_returns_exit_1` in `e2e_binary.rs`), and
the contract passes `every_contract_passes_positives_negatives_evasions`.
Short-term `bedrock-api-key-` keys stay out (body not authoritatively bounded).

_Original READY recipe (kept for traceability):_

**Gap:** keyhog has no detector for AWS Bedrock API keys. DET-17 lists
`aws-bedrock-api-key` as a real capability miss (`backlog/detection.md:211,231`).

**Format is authoritatively confirmed** (multiple corroborating sources +
GitHub Secret Scanning partner pattern since Aug 2025):
- Long-term key: **132 chars**, literal prefix `ABSK`, deterministic anchor
  `ABSKQmVkcm9ja0FQSUtleS` (this 22-char head is `ABSK` + base64("BedrockAPIKey"),
  so it is effectively unique → near-zero FP). AWS's own detection regex:
  `ABSKQmVkcm9ja0FQSUtleS[A-Za-z0-9+/]+={0,2}`.
- Short-term key: prefix `bedrock-api-key-` + base64 presigned URL (ephemeral;
  body charset/length NOT authoritatively bounded → ship long-term only, or
  anchor short-term on the literal with a bounded body).
- Sources: Wiz (`wiz.io/blog/a-new-type-of-long-lived-key-on-aws-bedrock-api-keys`),
  AWS docs (`docs.aws.amazon.com/bedrock/latest/userguide/api-keys.html`),
  GitGuardian detector page.

**No checksum obstacle:** the checksum registry
(`crates/scanner/src/checksum/mod.rs:46-53`) covers only github/npm/slack/pypi/
stripe/gitlab — Bedrock is not checksum-gated, so synthetic format-valid
fixtures are NOT silently dropped (cf. the fabricated-token memory note).

**Why it is not a 5-minute add — the count lock:** a dedicated detector is +1
to the embedded count. The Bedrock long-term key is its own credential type
(distinct format, no STS-V4 verify, no AKIA companions), so folding it into
`detectors/aws-access-key.toml` would mislabel it and is incoherent. A dedicated
`detectors/aws-bedrock-api-key.toml` is the correct home, but the "900" count is
**denormalized across 782 places** (see G9):
- `crates/scanner/tests/readme_claims.rs:46` — `assert_eq!(detectors.len(), 900)`.
- `README.md` — 8 occurrences (`:2` banner alt, `:19,50,60,80,224,254,571`) plus
  `docs/assets/keyhog-banner.svg`.
- **781** contract TOMLs stamp `readme_claim = "900 service-specific detectors"`,
  and `contracts_runner.rs:461 every_contract_readme_claim_present` requires that
  exact substring to live in `README.md`.

**Close recipe (count-coherent):**
1. Write `detectors/aws-bedrock-api-key.toml` (id `aws-bedrock-api-key`, service
   `aws`, severity `critical`, keywords `["ABSK","bedrock","bedrock-api-key"]`,
   pattern `ABSKQmVkcm9ja0FQSUtleS[A-Za-z0-9+/]{100,130}={0,2}`, `min_confidence
   = 0.2` defensively because the fixed anchor dilutes entropy scoring).
2. Write `crates/scanner/tests/contracts/aws-bedrock-api-key.toml` with synthetic
   format-valid fixtures (anchor + random 110-char `[A-Za-z0-9]` tail; four were
   pre-generated this session), negatives (`ABSK` without the anchor; anchor with
   <100-char tail), evasion (header/comment context), `readme_claim` = the NEW
   "901 service-specific detectors" phrase.
3. `README.md` 900→901 in all 8 spots; regenerate/edit banner SVG text.
4. `readme_claims.rs:38-47` 900→901 (and rename the test fn).
5. `sed` the 781 contract `readme_claim` stamps 900→901.
6. Verify: `cargo test -p keyhog-scanner --test contracts_runner` +
   `--test readme_claims` green; assert a planted ABSK key is found with
   `detector_id == "aws-bedrock-api-key"`.

**Blocker:** none technical (target files are disjoint from the in-flight scanner
refactor, G7). The only cost is the 782-file count churn, which is mechanical but
large. User interrupted before execution.

---

## G2 · cerebras-api-key (`csk-`) detector · DECLINED (need real sample)

**Gap:** DET-17 lists `cerebras-api-key` (`csk-` prefix) as missing
(`backlog/detection.md:212,230`).

**Why declined:** the exact body length/charset could NOT be confirmed from any
authoritative source — no trufflehog detector (`pkg/detectors/cerebras` returns
404), no gitleaks default rule, and web search explicitly returned only a
*guessed* `csk-[a-z0-9]{40,}` with a "validate against a real key" caveat.
Per the Screwdriver Principle (soundness over reach) and the backlog's own
stance ("deliberately NOT added from guessed formats", `detection.md:213-215`),
shipping a guessed shape is wrong.

**Asset needed to unblock:** one real (revoked) Cerebras `csk-` key, OR an
authoritative vendor/competitor regex, to bound body length + charset.

---

## G3 · no-prefix service detectors · DECLINED (need discriminator)

**Gap:** DET-17 misses sumologic, gitea, etsy, greptile, sidekiq-sensitive-url
(`backlog/detection.md:211-214,227-229`).

**Why declined:** these lack a distinctive vendor prefix. Shipping a bare
high-entropy-body regex for them would FP heavily. The backlog already marks
these as needing "a non-prefix discriminator before they can ship without an FP
cost." Correct stance; not a guessing target.

**Asset needed:** a non-prefix discriminator per service (surrounding key name,
host literal, or a labeled sample corpus to mine one).

---

## G4 · DET-17 stale claim: `gitlab-rrt` (`glrt-`) is NOT missing · RESOLVED (2026-06-10)

**Done.** `backlog/detection.md` DET-17 STATUS block now lists `gitlab-runner`
(`glrt-`, covering gitlab-rrt) and the newly-landed `aws-bedrock-api-key`
under LANDED, and drops both from STILL MISSING; `csk-` (cerebras) remains the
only prefix-anchored gap (needs a real sample).


**Gap (coherence):** `backlog/detection.md:211,230` lists `gitlab-rrt (glrt-)`
as a missing capability. It is **already shipped**:
- `detectors/gitlab-runner-authentication-token.toml:18,24` matches both the
  standard `glrt-` + 20-char form and the routable `glrt-t<n>_…` form.
- `glrt-` is in `crates/scanner/src/confidence/prefixes.rs:59` KNOWN_PREFIXES and
  validated by `crates/scanner/src/checksum/gitlab.rs:60-61`.

**Close recipe:** edit `detection.md` to move `gitlab-rrt`/`glrt-` from the
"STILL MISSING" list to the landed list. Pure doc correction, no code.

---

## G5 · DET-14 · decoy-aware generic assignment detector · LONG-HORIZON

**Gap:** `generic-high-entropy-string` scores **R=0.282 / FN=130** — the single
largest category recall deficit (`backlog/detection.md:169-181`). Fires on a
credential-keyword assignment but needs to reject decoy families (ARN /
docker-digest / npm-integrity / `sha256:`/`sha512-` / `<placeholder>`) WITHOUT an
FP explosion (a naive gate-exempt attempt added +1630 FP).

**Why long-horizon:** requires ML/context discrimination, not shape gates —
"shape gates alone lose real CredData secrets" (memory:
`base64-protobuf-precision-is-inseparable-from-recall`,
`real-world-recall-gap-creddata`). This is the dominant lever behind the real
CredData recall gap.

---

## G6 · DET-15 · base64 decode-attribution · LONG-HORIZON

**Gap:** keyhog decodes base64 and reports the **inner** value; ground truth in
k8s/JSON corpora is the **encoded literal**, so ~67 truly-detected secrets score
as FN (`backlog/detection.md:191-194`).

**Fix (not a suppression toggle):** ALSO report the encoded span (attribution)
alongside the decoded finding, so the reported credential matches the on-disk
literal. Genuine reporting/attribution work in the scanner result path.

---

## G7 · In-flight scanner refactor regressed phase-2 · BLOCKED (not mine)

**Gap:** an uncommitted Jun-10 refactor (158 files, +3649/-8290; deletes the GPU
subcrate `crates/scanner/src/engine/gpu_*.rs`, rewrites `megakernel.rs`+`scan.rs`)
regressed `scan_coalesced` phase-2: the kernel scan measured ~31 s vs the 4.25 s
fused baseline (earlier this session). `megakernel.rs` was last touched 20:23.

**Status:** this is another agent's / the user's active WIP — must NOT touch or
revert (CLAUDE.md: never destroy others' work). It blocks a clean fused-vs-legacy
perf re-measurement, and the on-disk release binary
(`/mnt/FlareTraining/santh-archive/cargo-target/release/keyhog`) is built from
this regressed tree (~31 s kernel) and is NOT representative.

**Close recipe:** once the refactor settles, rebuild and re-measure the fused
path on `/mnt/FlareTraining/santh-corpus/repos/linux`; confirm it returns to
~4.25 s / ~1833 % CPU and the 27-finding parity holds.

---

## G8 · backlog/dogfood-realtool.md not triaged · RESOLVED (2026-06-10)

**Triaged.** Every prior issue (DF-01/02/03/07) was already DONE; the one live
item was DF-04. Its original framing was stale: `--help` *does* carry an
`EXIT CODES` block and `docs/src/reference/exit-codes.md` exists. The real bug
was a coherence defect — both surfaces mislabeled source-backend / detector-
load failures as exit `3` when the code (`is_user_io_error` → 2;
`EXIT_SOURCE_FAILED = 2`; `diff`'s string-bail → 2) makes them `2`. Dogfooded
the binary (unknown flag / `--git-history` on non-repo / `diff` missing
baseline all → 2), fixed the doc + `--help`, and pinned the contract with
binary-driven tests (`scan_unknown_flag_exits_2`,
`scan_git_history_on_non_repo_exits_2`, `diff_missing_baseline_exits_2`).
DF-04 marked DONE.


**Gap:** `backlog/dogfood-realtool.md` was never triaged this session (carried
from the prior context as an open item). Unknown whether its findings are still
live against the current tree.

**Close recipe:** read it, reproduce each finding against the freshly-built
binary, encode any live one as a failing regression test, fix, rerun.

---

## G9 · Detector-count denormalization · RESOLVED (2026-06-10)

**Root-fixed.** The count no longer lives in 782 places. Removed the
`readme_claim = "900 service-specific detectors"` stamp from 781 contracts and
made the count DERIVE from `load_detectors()` in one place per surface:
`readme_claims::readme_claim_detector_count` (README headline + banner SVG),
`contract::readme_detector_count` (disk == loader, no literal), and the
`e2e_binary` banner test (binary == loaded corpus, consts removed). Adding a
detector now edits only the new TOMLs + the human README/banner. While doing
this, discovered + fixed a **dead gate** (see R2). Original analysis below.


**Gap:** the detector count "900" is duplicated into **782+ locations** — 781
contract `readme_claim` stamps, 8 README spots, the banner SVG, and the
`readme_claims.rs` assertion. Adding or removing a single detector forces an
~782-file churn (this is exactly what makes G1 expensive). Violates "one constant
source" (CLAUDE.md NO DUPLICATION; adversarial vector 7 DEDUPLICATION).

**Root fix (beyond G1):** derive the count from the loader in one place; make a
contract's `readme_claim` a *capability* string (e.g. the detector's own service
name) rather than the global count, so per-detector contracts stop re-stamping a
number they don't own. Then the README count becomes the single source and
`readme_claim_detector_count` reads it dynamically.

---

## G10 · Real-world CredData recall · LONG-HORIZON (meta-gap)

**Gap:** keyhog ranks 5th/6 on real CredData (F1 0.15, recall-bound) despite the
mirror benchmark at 0.92 (memory: `real-world-recall-gap-creddata`). The shipped
keyword-bridge reaches only ~10% of positives. DET-14 + DET-15 are the named
sub-levers; the meta-gap is that mirror F1 is NOT predictive of real recall.

**Discipline note:** always bench CredData, not just the mirror, before claiming
a recall win.

---

## G11 · Rust test sources committed as Git LFS pointers · RESOLVED (2026-06-10)

**Found via PR #8.** `.gitattributes` LFS-tracked real Rust source —
`crates/scanner/tests/readme_claims.rs` (the active README-claims suite) and
`crates/scanner/tests/diagnose_*.rs` (SIMD/GPU divergence diagnostics). In a
non-LFS checkout these materialize as pointer text, so `cargo check
--all-targets` fails parsing them as Rust. PR #8 tried to fix this by stubbing
the three real files out with empty `#[ignore]` placeholders (~540 lines of
lost coverage) and never touched `.gitattributes`, so its own guard would still
fail on fresh non-LFS clones — left it open with a REQUEST_CHANGES review.

**Fixed** (commit `030e56e7`, local — see G7-divergence note below): removed the
`*.rs filter=lfs` rules and re-added the three files as normal text (real
content preserved), plus a contract guard
(`contract/rust_test_sources_are_not_lfs_pointers`) that fails if any
`tests/**/*.rs` is an LFS pointer OR if `.gitattributes` LFS-tracks a `.rs`
path again — catching the root cause in both LFS and non-LFS checkouts. Guard
green (2/2).

**Landing blocker:** local `main` diverged from `origin/main` (11 real local
commits vs 6 `chore(metrics)` star-count bot commits) and the tree holds the
uncommitted G7 refactor, so a clean push needs a rebase the user must drive.
The fix is committed locally, ready to push once `origin/main` is reconciled.

## G12 · Contract `*.toml` fixtures + proptest seeds were LFS-tracked · RESOLVED (2026-06-10)

**Fixed** (commit `b34fae33`, local). De-LFS'd the entire contract corpus
(905 top-level + 177 companion `*.toml`) and the proptest-regressions seed —
verified all 1083 were real content in the working tree first (0 pointers, no
data loss), then removed the last `filter=lfs` rules so the repo now uses **no
LFS at all** (`git lfs ls-files` == 0). Extended the guard to cover `*.toml` +
`*.proptest-regressions` and to fail if `.gitattributes` LFS-tracks anything
under `crates/scanner/tests/` again. The de-LFS'd contract content also carries
the G9 readme_claim de-dup/relocation edits (folded in, since HEAD stored only
pointers). Same landing blocker as G11 (diverged `origin/main` + uncommitted
refactor) — committed locally, ready to push on reconciliation.

## G13 · Orphan contract `nih-pubmed-api` → vacuous negatives · RESOLVED (2026-06-10)

**Found by corpus-integrity audit.** `contracts/nih-pubmed-api.toml` declared
`detector_id = "nih-pubmed-api"`, but the real detector id is
`nih-pubmed-api-key`. The contract's positives still passed (they fire under
the real detector via `any_credential_contains`), but the negative loop keys
`detector_fired` on `detector_id` — so with an id that resolves to no detector,
**every negative passed vacuously** (the precision coverage was dead). Fixed
the contract id, and added `every_contract_detector_id_resolves` to
`contracts_runner` so any contract naming a non-existent detector fails loudly
(905 contracts now all resolve). Audit also confirmed: 901 detectors, **0
duplicate detector ids**, **0 detectors without a contract**.

## G14 · Duplicate contract files (one detector, two contracts) · OPEN (flagged, dedup)

Three detectors have two contract files each:
`npm-access-token` (`npm-token.toml` + `npm-access-token.toml`, **byte-identical**),
`data-gov-api-key` (`data-gov-api.toml` + `data-gov-api-key.toml`, **byte-identical**),
`github-pat-fine-grained` (`github-pat.toml` 2pos/1neg ⊂ `github-pat-fine-grained.toml`
3pos/4neg). The identical pairs run the same cases twice (pure redundancy). NOT
deleted: the old-stem filenames are cited as the filename≠id convention in
`all_detectors_self_validate.rs` / `detector_recall_prefixes.rs` /
`engine_backend_parity.rs` comments, so removing them is a convention call for
the maintainer. Close by deleting the redundant `*-` short-stem contracts and
updating those three comments.

## RESOLVED this session (kept for traceability)

### R1 · PERF-05 / PERF-08 · serial scan funnel · RESOLVED
The "kernel scan runs at ~2 of 32 cores / 84 s" framing is gone. The fused
parallel read+scan pipeline (`scan_sources_fused`,
`crates/cli/src/orchestrator/dispatch.rs:512`) is default for CPU/SIMD filesystem
scans; GPU keeps the legacy batch pipeline. Remeasured (pre-G7-regression):
fused **4.25 s / 1833 % CPU (~18 cores)** vs legacy 7.12 s / 749 %; thread scaling
1→32 = 9.6× (serial fraction ~0.075); finding set byte-identical at 27.
Guarded by `pipeline_fused_and_legacy_agree`, `pipeline_auto_filesystem_uses_fused`,
`scan_bigtree_completes_and_recalls` — all 12 orchestrator pipeline tests pass on
the current tree. Tier-A env knobs `KEYHOG_FUSED_BATCH`/`KEYHOG_FUSED_DEPTH`
added (default 32). Documented in `backlog/performance.md` (decode-recursion +
serial-funnel resolution sections). Residual lever: the 16→32 plateau is
`par_bridge` cursor-mutex + per-batch fork-join contention from `scan_coalesced`'s
own nested two-phase `par_iter`.

### R2 · `every_contract_readme_claim_present` was a dead gate · RESOLVED (2026-06-10)
Discovered while doing G9. A `readme_claim` written after a contract's
`[perf]`/`[scale]` header binds to that TOML table, not the Contract, so serde
silently dropped it — ALL six real claims parsed as `None` and the gate passed
vacuously (which is also why the bogus `readme_claim = "stripe"` never failed
against README's `Stripe`, and why the 781 count stamps were inert). Fixed:
moved the six claims to the top-level scalar position, corrected
`stripe`→`Stripe`, added `#[serde(deny_unknown_fields)]` to `PerfBudget`/
`ScaleBudget` (a future misplacement is now a loud parse error, not a silent
drop — Law 10), and added a `checked >= 6` liveness floor. The gate now checks
6 claims and is green; `every_contract_passes_positives_negatives_evasions` and
`adversarial_explosion_runner` still parse all 788 contracts.

### R3 · magic-count harness gates hid 6 orphan oracles + a dead contract subtree · RESOLVED (2026-06-10)
Started as "fix the brittle `gap_mod_has_ten_modules` test properly" (it broke
when the count moved 10→11). The proper refactor exposed a class bug. Two
a8-harness gates — `gap_mod_has_ten_modules` and the never-wired,
mis-named `contract_mod_has_ten_modules` (`contract_mod_eight_entries.rs`) —
asserted a **hardcoded count** of `pub mod` lines. A count gate breaks on every
legitimate add AND silently tolerates orphan test files whenever the number
happens to match. Both holes were live:
- **gap/** had **4 oracle files never registered** in `gap/mod.rs` (never
  compiled or run): `contract_tests_wired_in_all_tests`,
  `file_gate_matrix_core_columns_unmarked` / `..._missing_submodule_rows`
  (these two are **registered findings KH-GAP-177/178** whose registry notes
  even read "green" — coverage claimed but never executed, a Law-9 violation),
  and `sarif_fix_uri_percent_encodes_absolute_unicode`.
- **core contract/** had 2 orphan files (`sarif_schema_url_present`,
  `sarif_tool_driver_version_matches_crate`) AND the **entire `contract/`
  subtree was never wired into `all_tests.rs`** (no `pub mod contract;`), so 10
  SARIF/JSON contract tests never ran.

**Fix:** one shared correspondence invariant
(`integration/a8_harness/mod_wiring::assert_suite_fully_wired`) replacing both
count gates — a suite's `mod.rs` must register exactly its sibling `*.rs` files,
no orphans, no phantoms, no magic number. Wired all 6 orphans; added
`pub mod contract;`. Wiring **KH-GAP-178 surfaced the real drift it exists to
catch**: `crates/core/src/aws.rs` was missing from
`tests/FILE_GATE_MATRIX.toml` → added the row (truthful: `aws.rs` is covered by
`unit/aws.rs` + `new_core_types.rs`). Deleted the two dead count files. Core
`all_tests`: **349 pass, 0 fail** (was 333; +16 from the recovered oracles +
contract subtree + correspondence gates). Disjoint from the G7 scanner refactor;
the one needed untracked file (`gap/every_embedded_detector_parses.rs`, which
uses only HEAD-stable core APIs) was folded into the commit so the snapshot
compiles standalone.

### Local commits this session (on `main`, ahead of `origin/main`)
`63afa145` G1 Bedrock detector · `fbf6d2a2` G9/R2/G13 contract gates live ·
`3c8a728b` G8 exit-code doc · `5571c248` R3 correspondence gate + orphan wiring,
on top of the earlier `030e56e7` (G11 de-LFS `.rs`) and `b34fae33` (G12 de-LFS
fixtures). **Push still blocked** on the diverged `origin/main` (local commits
vs `chore(metrics)` bot commits) + the uncommitted G7 refactor in the shared
tree — reconciliation is the user's call; nothing here touches the refactor.

---

## Verification state (updated 2026-06-10, post-resolution)

Run with `--no-default-features --features ci-lean`, target dir
`/mnt/FlareTraining/santh-archive/cargo-target`:

- `readme_claims` (scanner): **7/7** — `readme_claim_detector_count` derives
  901 from the loader and matches README + banner.
- `contracts_runner`: `every_contract_passes_positives_negatives_evasions`,
  `every_contract_readme_claim_present` (now LIVE, 6 claims), and
  `contracts_cover_at_least_one_detector` PASS.
- `contract::readme_detector_count` (scanner all_tests): PASS (disk == loader).
- `adversarial_explosion_runner` (scanner): PASS — 788 contracts parse.
- `keyhog-core` `every_embedded_detector_parses` +
  `embedded_detector_count_matches_slice_len`: PASS (901 embedded).
- `e2e_binary` (cli): **30/30** — `readme_banner_counts_match_loaded_corpus`
  derives 901; `scan_finds_planted_bedrock_key_and_returns_exit_1`; the three
  exit-code pins (`scan_unknown_flag_exits_2`,
  `scan_git_history_on_non_repo_exits_2`, `diff_missing_baseline_exits_2`).
- Binary dogfood: a planted long-term key surfaces as
  `detector_id == "aws-bedrock-api-key"`, severity `critical`.

NOT re-run this pass (orthogonal, slow, or unaffected): the full scanner
`all_tests` sweep, the per-contract 1 MiB scale gate (time budgets aren't
enforced in debug anyway), and the G7 perf re-measurement (still blocked on the
in-flight scanner refactor — untouched here).

## Still open after this pass (unchanged status)

- **G2** cerebras `csk-`, **G3** no-prefix services — DECLINED, need a real
  sample / discriminator (soundness over reach; not guessable).
- **G5** DET-14 decoy-aware generic, **G6** DET-15 base64 attribution,
  **G10** real CredData recall — LONG-HORIZON ML/attribution work.
- **G7** in-flight scanner refactor perf regression — BLOCKED, not mine; the
  detector/contract/doc work above was kept disjoint from it.
