# Capability test skip baselines per host class.
# CI fails if a host class's skip count exceeds its committed baseline.
# Class H2 (GPU present and healthy) must execute all GPU tests (0 skips allowed).
[baselines]
H0 = 60
H1 = 30
H2 = 0
H3 = 30
H4 = 30
H5 = 30
