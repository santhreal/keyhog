# Docs: https://developer.dnsimple.com/v2/
# Format: OAuth2 Bearer token, alphanumeric string ~30 characters
# Verify: GET /v2/whoami with Bearer token returns 200 if valid
# Prefix: none (generic token, requires context anchoring)

[detector]
id = "dnsimple-api-token"
name = "DNSimple API Token"
service = "dnsimple"
severity = "critical"
keywords = ["DNSIMPLE", "dnsimple", "DNSIMPLE_API_TOKEN", "dnsimple_api_token"]

[[detector.patterns]]
regex = "(?:DNSIMPLE|dnsimple)(?:_API)?[_-]?(?:TOKEN|token|KEY|key)[=:\\s\"']+([a-zA-Z0-9_-]{20,50})"
description = "DNSimple API token with context anchor"
group = 1

[detector.verify]
method = "GET"
url = "https://api.dnsimple.com/v2/whoami"
allowed_domains = ["api.dnsimple.com"]

[detector.verify.auth]
type = "bearer"
field = "match"

[detector.verify.success]
status = 200

[[detector.verify.metadata]]
name = "account"
json_path = "$.data.account.email"

[[detector.tests]]
test_positive = "DNSIMPLE_API_TOKEN=C372xGw30nSx5QdQuTxy"
test_negative = "DNSIMPLE_API_TOKEN=short"
