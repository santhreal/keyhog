# Docs: https://docs.redis.com/latest/rc/api/get-started/manage-api-keys/
# Format: 40 character hex string (v2 API key format)
# Verify: GET /v1/subscriptions with x-api-key header
# Prefix: none (requires context anchoring for v2 format)

[detector]
id = "redis-cloud-v2-api-key"
name = "Redis Cloud V2 API Key"
service = "redis"
severity = "critical"
ml = { match_mode = "lift", entropy_mode = "disabled", weight = 1.0, context_radius_lines = 5 }
match_confidence = { literal_prefix_weight = 0.35, context_anchor_weight = 0.20, entropy_weight = 0.20, high_entropy_partial_weight = 0.12, moderate_entropy_threshold = 3.0, moderate_entropy_weight = 0.05, low_entropy_penalty_floor = 2.0, low_entropy_min_match_length = 10, low_entropy_penalty_multiplier = 0.60, keyword_nearby_weight = 0.10, sensitive_file_weight = 0.10, companion_weight = 0.05, very_high_entropy_margin = 1.2999999999999998, named_anchor_floor = 0.55, assignment_context_multiplier = 1.0, string_literal_context_multiplier = 0.9, unknown_context_multiplier = 0.8, documentation_context_multiplier = 0.3, comment_context_multiplier = 0.4, test_context_multiplier = 0.3, encrypted_context_multiplier = 0.05, soft_context_suppression_threshold = 0.5, encrypted_context_suppression_threshold = 0.8 }
# "x-api-key" removed: over-broad generic prefilter(s) the regex does NOT need, every
# pattern requires the service name as a leading anchor (kept), present in any real
# match and thus also the keyword_nearby signal. Verified recall-neutral.
keywords = ["REDIS_CLOUD_V2", "REDIS_V2", "redis_cloud_v2"]

[[detector.patterns]]
regex = "(?:REDIS_CLOUD_V2|REDIS_V2|redis_cloud_v2).{0,20}(?:API_KEY|api_key|api-key).{0,5}[=:\\s\"'']+([a-f0-9]{40})"
description = "Redis Cloud v2 API key with context anchor (40 hex chars)"
group = 1

[detector.verify]
method = "GET"
url = "https://api.redislabs.com/v1/subscriptions"
allowed_domains = ["api.redislabs.com"]

[detector.verify.auth]
type = "header"
name = "x-api-key"
template = "{{match}}"

[detector.verify.success]
status = 200

[[detector.tests]]
test_positive = "REDIS_CLOUD_V2_API_KEY=d1935adc5f2e5219091134d0fb1b5822c47225da"
test_negative = "REDIS_CLOUD_V2_API_KEY=YOUR_API_KEY_HERE_PLACEHOLDER_VALUE"
