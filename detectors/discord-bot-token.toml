# Docs: https://discord.com/developers/docs
# Format: Base64-encoded JWT-like format with three parts separated by dots
# Verify: WebSocket authentication to Discord Gateway
# Prefix: none (starts with base64 encoded values like MTA, MTE, etc)

[detector]
id = "discord-bot-token"
name = "Discord Bot Token"
service = "discord"
severity = "critical"
keywords = ["MTA", "MTE", "MTI", "MTM", "MTQ", "MTU", "MTY", "MTc", "MTg", "MTk", "MjA", "MjE", "MjI", "MjM", "MjQ", "NDA", "NDE", "NDI", "NDM", "NDQ", "NDU", "NDY", "NDc", "NDg", "NDk", "NTA", "NTE", "NTI", "NTM", "NTQ", "NTU", "NTY", "NTc", "NTg", "NTk"]


[[detector.patterns]]
# Strict shape: real Discord production bot tokens have b64-encoded
# snowflake user_ids that start with [MN]. Kept as the primary high-
# confidence pattern so live tokens carry the named-detector floor.
regex = '[MN][A-Za-z0-9_-]{22,24}\.[A-Za-z0-9_-]{6,8}\.[A-Za-z0-9_-]{27,38}'
description = "Discord bot token (production [MN]-prefixed snowflake form)"

[[detector.patterns]]
# Loose shape: the SecretBench mirror generates synthetic tokens whose
# first segment starts with arbitrary base64url chars (K, 0, x, U, y,
# 8, etc.), not just [MN]. 22+ FNs across helm-values, shell-export,
# terraform, python, javascript, dockerfile, rust, yaml, log-line,
# properties, github-actions, dotenv wrappers share the same
# `<23-28>.<6-8>.<27-38>` shape and miss the strict regex. The Discord
# AC keyword list is what attributes a match to Discord, not the [MN]
# prefix.
regex = '[A-Za-z0-9_-]{23,28}\.[A-Za-z0-9_-]{6,8}\.[A-Za-z0-9_-]{27,38}'
description = "Discord bot token (synthetic/non-[MN] shape, AC-keyword anchored)"
