# Docs: https://developer.tuya.com/en/docs/iot/open-api/overview
# Format: Access ID (20-char) and Access Secret (32-char) pair
# Verify: Sign API requests using Access ID, timestamp, and string-to-sign
# Prefix: none (requires context anchoring)

[detector]
id = "tuya-iot-api-credentials"
name = "Tuya IoT API Credentials"
service = "tuya"
severity = "high"
ml = { match_mode = "lift", entropy_mode = "disabled", weight = 1.0, context_radius_lines = 5 }
match_confidence = { literal_prefix_weight = 0.35, context_anchor_weight = 0.20, entropy_weight = 0.20, high_entropy_partial_weight = 0.12, moderate_entropy_threshold = 3.0, moderate_entropy_weight = 0.05, low_entropy_penalty_floor = 2.0, low_entropy_min_match_length = 10, low_entropy_penalty_multiplier = 0.60, keyword_nearby_weight = 0.10, sensitive_file_weight = 0.10, companion_weight = 0.05, very_high_entropy_margin = 1.2999999999999998, named_anchor_floor = 0.55 }
# "client_id", "client_secret" removed: over-broad generic prefilter(s) the regex does NOT need, every
# pattern requires the service name as a leading anchor (kept), present in any real
# match and thus also the keyword_nearby signal. Verified recall-neutral.
keywords = ["tuya", "access_id", "access_secret"]

[[detector.patterns]]
regex = '''(?:tuya|TUYA)[._-]?(?:ACCESS[_\-\s]*ID|access[_\-\s]*id|CLIENT[_\-\s]*ID|client[_\-\s]*id)[=:\s"'']+([a-z0-9]{20})'''
description = "Tuya IoT platform access ID"
group = 1

[[detector.patterns]]
regex = '''(?:tuya|TUYA)[._-]?(?:ACCESS[_\-\s]*SECRET|access[_\-\s]*secret|CLIENT[_\-\s]*SECRET|client[_\-\s]*secret)[=:\s"'']+([a-f0-9]{32})'''
description = "Tuya IoT platform access secret"
group = 1

[[detector.tests]]
test_positive = "tuyaaccess-id   =\"\"   \" = \"  : ''     :    \"  =\" = \"' \" \" ':  1l24ig2ph6ekd7s85fd0"
test_negative = "tuyaaccess-id   =\"\"   \" = \"  : ''     :    \"  =\" = \"' \" \" ':  YOUR_API_KEY_HERE_PLACEHOLDER_VALUE"
