# Docs: https://www.checkout.com/docs/developer-resources/api/manage-api-keys/api-keys
# Format: sk_sbox_ or sk_ for secret, pk_sbox_ or pk_ for public, followed by alphanumeric
# Verify: GET to api.checkout.com with Bearer token
# Prefix: sk_sbox_, sk_, pk_sbox_, pk_

[detector]
id = "checkout-com-api-key"
name = "Checkout.com API Key"
service = "checkout-com"
severity = "critical"
ml = { match_mode = "lift", entropy_mode = "disabled", weight = 1.0, context_radius_lines = 5 }
match_confidence = { literal_prefix_weight = 0.35, context_anchor_weight = 0.20, entropy_weight = 0.20, high_entropy_partial_weight = 0.12, moderate_entropy_threshold = 3.0, moderate_entropy_weight = 0.05, low_entropy_penalty_floor = 2.0, low_entropy_min_match_length = 10, low_entropy_penalty_multiplier = 0.60, keyword_nearby_weight = 0.10, sensitive_file_weight = 0.10, companion_weight = 0.05, very_high_entropy_margin = 1.2999999999999998, named_anchor_floor = 0.55, assignment_context_multiplier = 1.0, string_literal_context_multiplier = 0.9, unknown_context_multiplier = 0.8, documentation_context_multiplier = 0.3, comment_context_multiplier = 0.4, test_context_multiplier = 0.3, encrypted_context_multiplier = 0.05, soft_context_suppression_threshold = 0.5, encrypted_context_suppression_threshold = 0.8, post_match = { placeholder_multiplier = 0.05, minimum_byte_diversity = 0.1, low_diversity_multiplier = 0.1, maximum_repeat_ratio = 0.8, degenerate_run_min_length = 10, degenerate_repeat_multiplier = 0.1, fixture_path_multiplier = 0.5, ml_context_reapply_below = 0.95 } }
keywords = ["checkout", "checkout.com", "cko"]

# Sandbox keys have specific prefixes that are unlikely to match false positives
[[detector.patterns]]
regex = "sk_sbox_[a-z0-9]{24,}"
description = "Checkout.com sandbox secret key"

[[detector.patterns]]
regex = "pk_sbox_[a-z0-9]{24,}"
description = "Checkout.com sandbox public key"

[[detector.patterns]]
regex = "ack_sbox_[a-z0-9]{8,}"
description = "Checkout.com sandbox access credential ID"

# Production keys need context anchoring since sk_, pk_, ack_ are too generic alone
[[detector.patterns]]
regex = "(?:checkout|cko)[\\s]*[=:]+[\\s]*[\"\\']*(sk_[a-z0-9]{24,})"
description = "Checkout.com production secret key with context"
group = 1

[[detector.patterns]]
regex = "(?:checkout|cko)[\\s]*[=:]+[\\s]*[\"\\']*(pk_[a-z0-9]{24,})"
description = "Checkout.com production public key with context"
group = 1

[[detector.patterns]]
regex = "(?:checkout|cko)[\\s]*[=:]+[\\s]*[\"\\']*(ack_[a-z0-9]{8,})"
description = "Checkout.com production access credential ID with context"
group = 1

[detector.verify]
method = "GET"
url = "https://api.sandbox.checkout.com/payments?limit=1"
allowed_domains = ["api.sandbox.checkout.com"]

[detector.verify.auth]
type = "bearer"
field = "match"

[detector.verify.success]
status = 200

[[detector.tests]]
test_positive = "sk_sbox_kp4qx7rm2sn5tb8vw3yzkp4qx"
test_negative = "sk_sbox_short"
