# Docs: https://documentation.idenfy.com/
# Format: Basic Auth with API Key (username) and API Secret (password)
# Verify: no public verification endpoint
# Prefix: none (requires context anchoring)

[detector]
id = "idenfy-api-credentials"
name = "iDenfy API Credentials"
service = "idenfy"
severity = "high"
# "api_key" removed: over-broad generic prefilter the regex does NOT need — every
# pattern self-anchors on the service name (kept), which is present in any real
# match and so also carries the case-sensitive keyword_nearby signal. Recall-neutral.
keywords = ["idenfy", "IDENFY", "api_secret"]

[[detector.patterns]]
regex = "(?:idenfy|IDENFY)[_\\s]*(api[_\\s]*key)[=:;\\s\"'']+([a-zA-Z0-9_-]{10,})"
description = "iDenfy API key with context anchor"
group = 2

[[detector.companions]]
name = "idenfy_api_secret"
regex = "(?:idenfy|IDENFY)[_\\s]*(api[_\\s]*secret)[=:;\\s\"'']+([a-zA-Z0-9_-]{20,})"
within_lines = 5

[[detector.tests]]
test_positive = "idenfy api_key \"H_ZM9TBrKrmGsNmjQ8mT3OA9\""
test_negative = "idenfy=short"
