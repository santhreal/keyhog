category = "Performance"
summary = "CPU scans now reuse path-independent entropy absence proofs for byte-identical repeated payloads and invalidate them when entropy policy changes."
crates = ["scanner"]
