category = "Fixed"
summary = "Detector-owned reverse and Caesar prefix gates use compact contiguous automata, reducing packed-scan startup ownership without changing decode selection."
crates = ["scanner"]
