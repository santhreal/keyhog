category = "Fixed"
summary = "Installed scans hydrate authenticated detector plans directly from execution packs instead of rebuilding the detector schema graph. Sparse files stream only allocated extents and report all-hole files as uncovered regions, fused source boundaries default to rendezvous channels, and homoglyph prescreening no longer materializes Unicode matchers for unrelated replacement characters."
crates = ["scanner", "sources", "cli"]
