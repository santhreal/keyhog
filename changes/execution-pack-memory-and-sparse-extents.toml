category = "Fixed"
summary = "Installed scans stream authenticated detector plans from execution packs without decoding detector schemas, share compiled signature strings with post-processing, and compile companion regexes only when a primary match consults them. Phase-two no-candidate gates are scoped to the active residual route and confirmed anchors retain a compact automaton. Sparse files stream only allocated extents and report all-hole files as uncovered regions, stdin validates its byte cap through an anonymous spool before scanning bounded overlapping windows, fused source boundaries default to rendezvous channels, and homoglyph prescreening no longer materializes Unicode matchers for unrelated replacement characters."
crates = ["scanner", "sources", "cli"]
