category = "Fixed"
summary = "Unbounded Unix filesystem discovery releases its final in-memory sort slabs before mapping the external merge spool, lowering peak RSS without changing enumeration."
crates = ["sources"]
