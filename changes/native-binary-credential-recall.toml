category = "Fixed"
summary = "Keep complete credentials from native binary strings and executable sections when a strong named detector validates an explicit credential shape, while continuing to suppress weak prefix fragments and generic assignment noise."
crates = ["scanner"]
