category = "Fixed"
summary = "Include known reason and repair command in daemon warm-route errors instead of hiding them behind a generic fallback. Apply the same fix to the startup banner. Make is_work_request exhaustive so adding a new Request variant causes a compile error. Add 29 regression tests pinning daemon server pure-function behaviors before modularization."
crates = ["cli"]
