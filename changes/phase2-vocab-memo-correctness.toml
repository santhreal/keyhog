category = "Fixed"
summary = "Windowed vocab absence memos now fingerprint ordered line sequences (not unique-line sets), only engage for parent filesystem/windowed slices, reuse the batch entropy config digest, and drop new keys at capacity instead of clearing unrelated proofs."
crates = ["scanner"]
