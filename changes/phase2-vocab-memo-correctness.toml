category = "Fixed"
summary = "Windowed vocab absence memos keep unique-line fingerprints for overlapping-window hits, only engage for parent filesystem/windowed slices, reuse the batch entropy config digest, and drop new keys at capacity instead of clearing unrelated proofs."
crates = ["scanner"]
