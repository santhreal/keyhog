category = "Performance"
summary = "CPU scans now reuse exact phase-two keyword and generic-assignment localization evidence computed for byte-identical autoroute payload representatives."
crates = ["scanner"]
