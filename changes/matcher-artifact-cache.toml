category = "Performance"
summary = "Cold one-shot and incremental scans now reuse a persisted MatcherArtifact of the eager compiled matcher graph across process invocations, with CacheId hit/miss/invalidation in profile output and fail-closed identity checks."
crates = ["scanner", "cli"]
