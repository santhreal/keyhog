category = "Performance"
summary = "Docker image scans now stream metadata and rewritten layer chunks through a one-row backpressure channel instead of retaining all extracted layer content until the image finishes."
crates = ["sources"]
