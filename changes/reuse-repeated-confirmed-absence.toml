category = "Performance"
summary = "CPU scans now reuse exact confirmed-pattern absence proofs for byte-identical repeated payloads instead of rerunning confirmed regexes per chunk."
crates = ["scanner"]
