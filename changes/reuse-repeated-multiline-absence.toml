category = "Performance"
summary = "CPU scans now reuse exact multiline-admission absence proofs for byte-identical repeated payloads and invalidate them when evidence policy changes."
crates = ["scanner"]
