category = "Performance"
summary = "CPU scans now reuse exact always-active phase-two absence proofs for byte-identical repeated payloads instead of rerunning the shared prefilter per chunk."
crates = ["scanner"]
