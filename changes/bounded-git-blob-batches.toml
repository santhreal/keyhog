category = "Performance"
summary = "Git history scans now yield each bounded decoded-blob batch before loading the next batch instead of retaining every decoded payload from a commit."
crates = ["sources"]
