category = "Performance"
summary = "Execution-pack startup now borrows detector-plan prelude strings directly from authenticated framed rows while interning runtime ownership, avoiding transient per-row string copies."
crates = ["scanner"]
