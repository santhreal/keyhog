category = "Performance"
summary = "Persistent daemons now configure KeyHog's physical-core Rayon pool before detector loading, preventing an accidental logical-core pool and its duplicate resident worker stacks."
crates = ["cli"]
