[package]
name = "keyhog-ci-operability"
version = "0.0.0"
publish = false
edition = "2024"

# Standalone — NOT part of the keyhog workspace. Reads repo-root
# workflow YAML from coordination/ci-operability/tests/ without
# pulling in keyhog crate dependencies.
[workspace]

[dev-dependencies]
