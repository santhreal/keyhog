# LR1-A10

LOCK: `.github/workflows/**`, `benches/**`, `scripts/**`, `coordination/**`, `fuzz/**`, `GAP_FINDINGS.toml`

## Hunt

- finding count: **7** (KH-GAP-011 … KH-GAP-017)
- audited: 8 workflow YAMLs, `TESTING_PROGRAM.md` §3.1, `runners-nightly.yml` vs `ci.yml` strict-runners split
- distcc: **zero** references to `env -u CC`, `unset CC`, or distcc anywhere in repo workflows
- Hyperscan: Linux PR/nightly install `libhyperscan-dev`; macOS/Windows/release portable skip simd; `secretbench-nightly.yml` builds default features without Hyperscan install
- strict env: PR sets 3 multiplier vars; nightly sets 11; 10 runners PR-deferred

## Tests added

- count: **7** (`coordination/ci-operability/tests/gap/`)
  - `pr_ci_runs_all_fourteen_strict_runners.rs` — RED (KH-GAP-011)
  - `workflows_unset_cc_distcc_guard.rs` — RED (KH-GAP-012)
  - `secretbench_missing_hyperscan_install.rs` — RED (KH-GAP-013)
  - `macos_ci_skips_hyperscan_default_build.rs` — RED (KH-GAP-014)
  - `fuzz_not_wired_in_ci.rs` — RED (KH-GAP-015)
  - `nightly_matrix_has_fourteen_runner_binaries.rs` — green invariant
  - `nightly_exports_all_strict_env_vars.rs` — green invariant

## Deliverables

| Artifact | Path |
|----------|------|
| 14-runner CI matrix | `coordination/rounds/LR1-ci-matrix.md` |
| Operability test crate | `coordination/ci-operability/` |
| Findings registry | `GAP_FINDINGS.toml` (KH-GAP-011–017) |

## Commands

```text
env -u CC cargo test --manifest-path coordination/ci-operability/Cargo.toml
→ 7 tests: 2 passed, 5 failed (intentional RED gap tests)
```

## GAP_FINDINGS appended

- KH-GAP-011 PR CI gates only 4/14 strict runners
- KH-GAP-012 No `env -u CC` distcc guard in CI workflows
- KH-GAP-013 secretbench-nightly builds without Hyperscan dev libs
- KH-GAP-014 macOS CI skips default-features Hyperscan path
- KH-GAP-015 fuzz/ targets not wired into CI
- KH-GAP-016 companion_contracts_runner parity issues warn-only
- KH-GAP-017 cve_replay_runner vacuous pass when corpus empty

## Key findings (summary)

1. **PR strict subset (4/14):** `ci.yml` `strict-runners` runs only contracts + adversarial + encoding + path_shape; remaining 10 multipliers are nightly-only (~45 min deferral documented in workflow comments).
2. **distcc guard absent:** No workflow unsets `CC` before `cargo build/test/check`; local distcc/ccache wrappers can diverge from CI (KEYHOG_LINUX_QUALITY_PROGRAM mandates `env -u CC`).
3. **Hyperscan holes:** macOS CI intentionally `--no-default-features`; secretbench nightly builds release keyhog without installing libhyperscan-dev (likely links fallback or fails silently on simd).
4. **Fuzz unwired:** `fuzz/` has 4 targets (`scanner_target`, `decode_target`, etc.) but no workflow invokes `cargo fuzz`.
5. **Soft gates:** `companion_contracts_runner` prints parity issues to stderr without failing; `cve_replay_runner` passes vacuously when `tests/cve_replay/` is empty.

## Files changed

- `coordination/rounds/LR1-ci-matrix.md` (new)
- `coordination/rounds/LR1-A10.md` (new)
- `coordination/ci-operability/Cargo.toml` (new)
- `coordination/ci-operability/tests/all_tests.rs` (new)
- `coordination/ci-operability/tests/gap/mod.rs` (new)
- `coordination/ci-operability/tests/gap/*.rs` (7 new)
- `GAP_FINDINGS.toml` (KH-GAP-011–017 appended)
