# 99 — Execution ledger

One line per landed item. Append-only. Format:
`YYYY-MM-DD · KH-L-NNNN/KH-G-NNNNN · commit · axes-moved · proving-test`

Axes (Scale Law): detection · perf · test-depth · dogfood · org/dedup · architecture.
A landed item names ≥1 axis and points at the proving test/PoC. Coverage notes
(what was covered, what's still unreached) go here, never to chat. "Clean/done"
is never written — only specific proven outcomes.

## Plan authored
- 2026-06-11 · plan · (docs/legendary) · org/architecture · 273 KH-L lane items + 3866 KH-G materialized items = 4139 concrete; materializer `scripts/materialize-backlog.sh` regenerates `99_BACKLOG.tsv` from the live registries.

## Pre-plan landings this session (engine refactor lineage)
- 2026-06-11 · (TUI) · 3de7a467,00f65606 · dogfood/test-depth · idle TUI ~0% CPU + feed==reporter-dedup.
- 2026-06-11 · KH-L-(homoglyph) · b5526b71 · detection/perf/test-depth · overlapping-AC + ASCII-skip default ON; parity+shadow gates in CI; ~13% faster, +14 mailgun recovered.
- 2026-06-11 · (determinism) · 80602457 · test-depth/architecture · push_match eviction order-independence pinned (RawMatch::Ord totality) + core file wired into CI.
- 2026-06-11 · KH-L-(stale-gates) · 8db8b347,dcc60edf · org/test-depth/architecture · megakernel inline-test allowlist (+stale guard) + 9 GPU contract gates re-pointed to the consolidated dispatch; behaviors verified preserved, not relaxed.

## Backlog burndown
- KH-G todo: 3866 / done: 0  (run `scripts/legendary-status.sh` once built — KH-L-0030)

## Execution log (newest last)
<!-- append landings below -->
- 2026-06-11 · KH-L-0001 · (root-cause, no code) · architecture · **vyre 0.6.2 release block = no single version source.** `[workspace.package]` (vyre/Cargo.toml:65) sets edition+rust-version but NOT `version`; 28 vyre crate manifests carry literal `version = "0.6.1"` (only other fields inherit via `.workspace`), `[workspace.dependencies]` re-pins each vyre-* to `"0.6.1"` (Cargo.toml:186-192), and `release/release-train.toml` independently hardcodes `[versions]`, all RC/final `[tags]`, 10 `required_release_note_tokens`, and `package_verify_passed`. Cutting 0.6.2 = lockstep-editing 28 manifests + the deps block + release-train + readiness, with `check_crate_metadata_normalized.sh`/`check_release_ready.sh` failing on any miss. CAVEAT: the workspace is multi-product — weir/vyrec/vyrec_train are at 0.1.0/0.1.0-beta, so the fix must convert ONLY the 28 vyre-0.6.1 crates, never blanket-inherit. NEXT: KH-L-0002 = add `[workspace.package] version`, convert the 28 vyre crates to `version.workspace = true`, bump deps+release-train to 0.6.2, coordinate keyhog's path override `=0.6.1`→`=0.6.2` (stays uncommitted until publish), prove BOTH trees build. KH-L-0006 (cargo publish) stays gated (irreversible + standing rule).
- 2026-06-11 · KH-L-0001 (empirical proof) · (ran vyre `xtask version-matrix`) · architecture · CORRECTION: vyre already HAS full release machinery (`xtask version-matrix`/`release-train`/`release-gate`/`release-evidence`/conformance+signed certs); the block is NOT a missing tool. `check_crate_metadata_normalized.sh` deliberately omits `version` from the inherit set (multi-product: weir/vyrec at 0.1.0), so `version.workspace=true` is the WRONG fix. Running `version-matrix` surfaces the real first blocker: **`release/release-train.toml` fails the tooling's schema** — "missing field `required_release_note_tokens`" (it sits under `[tags]` but the release_train deserializer expects it at another level). So KH-L-0002 starts by reading `xtask/src/release_train.rs` (the expected struct), fixing release-train.toml's schema, then a lockstep `bump-vyre 0.6.2` (28 vyre crates + workspace.deps + tokens/tags, weir/vyrec untouched), re-run version-matrix to zero blockers, prove vyre+keyhog build with keyhog's override flipped `=0.6.1`→`=0.6.2` (uncommitted). Publish stays gated.
- 2026-06-11 · KH-L-0002 (partial — version-matrix GREEN) · vyre 66481599eb + afd3e00518 · architecture · Drove vyre's OWN release machinery to first-gate-green at 0.6.1: (1) hoisted `required_*`/`package_verify_passed` above `[tags]` in release-train.toml (TOML was binding them into [tags], so the ReleaseTrainData deserializer saw them missing and `version-matrix` couldn't parse at all); (2) synced `vyre-grammar-gen` 0.6.0→0.6.1 (folded-in crate left at old version) + relocked. `xtask version-matrix` now exits 0, blockers `[]`. The release tooling parses + runs end-to-end; workspace is version-consistent at 0.6.1. PARKED: the 0.6.2 lockstep bump only has meaning in the same motion as `cargo publish` (it forward-dates release-note tokens, RC tags, 28 manifests), and publish is a USER-ONLY irreversible asset (standing rule). The hard engineering (the unreleased `build_regex_dfa_unanchored` + `scan_presence_by_region`) is already in source. Lane correctly stopped at the user-asset boundary; pivoted to a fully-ownable detection lane.
- 2026-06-12 · KH-L-0100/0101 · benchmarks/bench/creddata_miss_analysis.py · detection/test-depth/dogfood · Stood up the CredData miss-ledger + candidate-detector eval tool (`shapes`: per-category value-shape × keyword precision ceiling; `simulate`: apply a candidate line-regex to the real corpus, score with the bench's own `overlap` rule in seconds — no rebuild). Ground truth: keyhog recall on REAL CredData is 0.076 (F1 0.129), recall-bound; the misses concentrate 82% in 6 generic context-identified categories (Key 3795, UUID 2263, Password 2233, Secret 1187, Basic-Auth 596, Token 429), NOT provider tokens (those keyhog nails).
- 2026-06-12 · KH-L-0102 (basic-auth landed) · detectors/basic-auth-credentials.toml + tests/basic_auth_credentials_recall.rs · detection/test-depth · `basic-auth-credentials` (`Basic <base64{16+}>`) closes the Auth:Basic-Authorization category that was 100% uncaught: CredData Basic-Auth 0→491 TP (R 0.824), 15 FP. Net measured (902 detectors, simd, KEYHOG_NO_GPU=1): **CredData F1 0.129→0.254, R 0.076→0.163, P 0.423→0.571; mirror F1 0.9131→0.9183, P 0.9945→0.9953 (IMPROVED, no regression).** Proof: per-category Basic-Auth TP/FP in the result JSON + the recall contract test.
- 2026-06-12 · KH-L-0103 (MoE retrain — MEASURED, reverted, finding documented) · ml/retrain_loop.sh (measure-only + gated --write, then restored .bak) · detection/ml/architecture · Ran the full real-distribution retrain loop: harvest CredData with the shipped binary (3925 labeled records, 2361 pos / 1564 neg via the bench's ground-truth overlap) → blend with 23040 synthetic, file-grouped split (no leakage) → retrain 42-feature MoE. **KEY FINDING: real-distribution training SOLVES the key-vs-hash ambiguity** — probe scores `real hex key` 0.887 / `real ghp PAT` 0.99 / `anchored secret` 0.974 (HIGH) vs `md5` 0.018 / `sha256` 0.019 / `placeholder` 0.007 / `base64-PNG` 0.007 / `prose token` 0.025 (LOW). Real held-out recall 0.904, synthetic F1 held 0.960. Gates passed; shipped + rebuilt + benched BOTH corpora. Result: mirror HELD (P 0.9953 identical, F1 0.9179 vs 0.9183), CredData F1 0.2539→0.2584 (+recall 81 TP) BUT a concentrated precision regression — **`kubernetes-bootstrap-token` +203 FP (5→208)** on CredData (the model over-promotes the `[a-z0-9]{6}\.[a-z0-9]{16}` shape; mirror lacks that trap so it held). Reverted to the battle-tested `weights.bin` (Law 1: a +0.0045 F1 doesn't justify a 203-FP per-detector spike). NEXT: the retrain is reproducible and the key-vs-hash capability is real — to ship, mine the CredData kubernetes-bootstrap-token (+ nats-credentials +42) negatives into the training set as hard negatives, re-gate per-detector FP (not just aggregate F1), THEN the model can carry a re-introduced keyword-hex detector that the crude `is_encoded_binary` penalty cannot. The miss-analysis tool + harvested `ml/data/real_corpus.jsonl` are in place for the next loop.
- 2026-06-12 · KH-L-0102 (generic-crypto-key — DISK-DOCUMENTED KILL, reverted) · detection/architecture · A `<keyword> = <32/48/64 hex>` "Key" detector measured 0.92-1.0 precision on CredData but is fundamentally **key-vs-hash ambiguous**: on the mirror (sha256/md5 FP-trap corpus) it fired **1058 FP / 54 TP**, collapsing mirror P 0.9945→0.71 (F1→0.78). Length is not a disambiguator (64=sha256, 32=md5; only 48=AES-192 is non-hash, and adds ~nothing real). 95% of CredData "Key" positives are OpenSSL crypto TEST VECTORS that keyhog's MoE correctly scores ~0.25 and drops (surfacing them = overclaiming a leak, Screwdriver Principle). Reverted the detector + its 4 supporting changes (shape-gate bypass, `detector_carries_positive_shape_evidence`, entropy-floor arm). LESSON (memory: keyword-hex-is-key-vs-hash-ambiguous): NEVER ship on a CredData-only measurement — bench BOTH corpora + per-detector FP. The real keyword-hex fix is ML context, deferred to the MoE-retrain lane (KH-L-0103).
- 2026-06-12 · KH-L-0103 (harvest-floor — root-cause + fix) · 84014725 · architecture/ml/test-depth/dogfood · Root-caused the +203-FP retrain regression to a HARVEST BLIND SPOT: `ml/harvest_corpus.py` scanned with the default ~0.30 report floor, so every candidate a detector fires on but scores sub-floor was absent from the training corpus — a retrain could never learn the hard negatives it surfaces only as below-threshold scores, and was free to over-promote them. Fix: harvest-only `ScannerConfig.min_confidence` → `--min-confidence` (threaded in keyhog adapter `_cmd`, OFF the leaderboard matrix key so config_id/to_json are byte-identical for every scored config) + `--harvest-floor` (default 0.0). Proven at scale: default-floor harvest captured **0** kubernetes-bootstrap-token-shape negatives, floor-0.0 captured **55**; record count 3925 (2361/1564) → 5041 (2738 pos / **2303 neg, +739 hard negatives**). 67 bench tests green (incl. new schema + adapter round-trip/threading tests).
- 2026-06-12 · KH-L-0103 (per-detector FP gate) · b0ca9417 · architecture/test-depth · `bench gate` only inspected `detection.overall`, so a single-detector +203-FP spike masked by aggregate recall (overall CredData F1 even *rose* 0.2539→0.2584) passed every floor + the baseline-F1 check. Added a per-detector FP-regression check that runs whenever `--baseline` is given: flag a detector when its FP exceeds baseline by BOTH an absolute floor (def 20, ignores corpus noise) AND a relative floor (def 1.0, tolerates proportional drift); a detector absent from baseline appearing >abs is flagged new. Tunable `--max-detector-fp-abs/-rel`, skippable `--no-detector-fp-regression`. Refactored to one `_baseline_keyhog_row` (F1 + FP baseline can't disagree). Now LIVE in differential-bench CI on mirror (verified current 902-detector model passes: max baseline FP 7 ≪ 20). 72 bench tests green; the 5040→208 shape is the central proving test.
- 2026-06-12 · KH-L-0103 (megakernel test cfg-gate) · 03211f1e · test-depth/architecture(coherence) · `cargo test -p keyhog-scanner` failed to compile under non-gpu feature sets — 5 GPU-only megakernel/fallback test files imported `vyre_driver_wgpu`/`vyre_runtime` unconditionally (those crates are `dep:`-gated behind `gpu`), aborting the whole run before any test. Added `#![cfg(feature = "gpu")]` (the existing gpu-test idiom). Verified full-crate `--features ci-lean --no-run` Finishes clean (0 vyre errors); named CI targets unaffected.
- 2026-06-12 · KH-L-0103 (retrain-loop --verify wiring) · c7f796ae · architecture/ml · `ml/retrain_loop.sh` shipped behind train-gates only (the guard that the +203 spike passed). Added fail-closed `--write --verify`: self-baseline the current model on mirror+creddata BEFORE shipping → ship → rebuild → re-bench → `bench gate --baseline <pre-ship> --no-beat-competitors` (per-detector FP + F1); any regression → restore weights.bin from .bak + rebuild known-good, exit non-zero. Bare `--write` prints a loud "verify not run" banner — never silently ships unverified.
- 2026-06-12 · KH-L-0103 (MoE retrain — SHIPPED with the corrected harvest) · 93590559 · detection/ml · Re-ran the retrain on the floor-0.0 harvest (the +739 hard negatives, incl. 55 k8s-token-shape). Train-gates: synthetic held-out F1 0.9635, real held-out F1 0.8458 / recall@0.40 0.8412. Key-vs-hash SOLVED: probe `real hex key` 0.87 / `ghp PAT` 0.988 / `anchored` 0.978 (HIGH) vs `md5` 0.016 / `sha256` 0.017 / `base64-PNG` 0.006 / `prose` 0.027 (LOW). Verified via the new --verify path against a self-captured baseline: **kubernetes-bootstrap-token 5→1 (NOT +203)** — the harvest-floor fix eliminated the regression. CredData TP 2272→**2386 (+114)**, FP 1704→**1672 (−32)**, F1 0.2539→**0.2655**; mirror P **0.9953 held** (F1 0.9183→0.9177, within ε). Both per-detector FP gates PASS (max delta +6). Full weights-sensitive contract suite (contracts_runner/companion/cve_replay/adversarial + lib) 0 failures. Model `moe-v1-7c64faeb77749eb0` → `moe-v1-8c4d64ffb8fed94e`. This RESOLVES the open key-vs-hash item in KH-L-0102.
- 2026-06-12 · KH-L-0080 (navigability — the whole pipeline on one page) · 27e19b34,27b692d8,f58443eb,224aee1f · org/architecture(coherence) · "Understand the whole pipeline" had no single entry: 17 top-level dirs + 5 crates, and the deepest flow doc was buried in `engine/mod.rs`. Added **`docs/ARCHITECTURE.md`** — every top-level dir (one line, code-vs-DATA made explicit: `detectors/`+`rules/` are data), the enforced one-way crate DAG (`core ← verifier ← {scanner,sources} ← cli`) as a diagram + per-crate "start reading at", and the bytes→finding pipeline (6 stages, each → the owning module, linking `engine/mod.rs` as the method-level source of truth not restating it). README's Architecture section links it. Made **`scanner/lib.rs`** a pipeline-ordered module map (40+ modules grouped config→prefilter→compile→engine→decode→entropy→ML→suppression→validators) and added the missing **`cli/lib.rs`** crate-doc map (17 modules by responsibility). Surfaces (not hides) the two-planning-tree fact (`docs/legendary/` wins over `backlog/`). Pure navigation; no code/decls moved.
- 2026-06-12 · KH-L-0080 (coverage note — physical module reorg DEFERRED with reason) · (no code) · architecture · [SUPERSEDED 2026-06-13 — reorg COMPLETED, see entry below] The scanner/src "loose file" prefix-groups (`gpu*`, `ml_*`, `entropy_fast*`, `compiler_*`) read as flat soup in `ls` but ARE logically grouped via parent modules + now fully mapped in `lib.rs`. Converting them to subdirectories (`gpu/`, `ml/`, fold `entropy_fast*`→`entropy/`, `compiler/`) is the obvious next clarity step BUT is NOT safely verifiable on this x86/non-GPU host: `entropy_fast_neon` is `#[cfg(target_arch="aarch64")]` (cannot compile here at all), the `gpu` group needs the wgpu/vyre GPU stack, and `weights.bin`/`OUT_DIR` `include!` paths move with `ml_*`. Half-doing it (only the verifiable `ml`+`compiler`) would make the layout INCONSISTENT — worse for clarity than uniform-flat. Correct no-regret call (Law 1): do all four as ONE batch under GPU+ARM CI verification, not piecemeal blind. Also tracked: ~22 pre-existing feature-conditional broken intra-doc links in scanner (no CI doc gate today) — hygiene debt for a `cargo doc -D warnings` pass.
- 2026-06-13 · KH-L-0080 (physical module reorg — COMPLETED, all four groups under GPU+ARM verification) · a6e7b316,f5c24ab5,464a2fe0,d8711905 · org/architecture(coherence) · The deferral above is resolved: the four scanner/src prefix-groups are now subdirectories, removing every `foo_*.rs` cluster from the crate root (`ls crates/scanner/src` now shows `compiler/ gpu/ ml_scorer/ entropy/` dirs, zero loose siblings). Done as the deferred no-regret batch, each verified before the next: **ml** (`ml_scorer.rs`+`ml_scorer/{ml_weights,ml_features}.rs`; `weights.bin` `include_bytes!` repointed `../weights.bin`, `OUT_DIR` `include!` unchanged), **compiler** (`compiler/{compiler_build,compiler_compile,compiler_prefix}.rs`), and **gpu** (`gpu/{gpu_shader,backend,env}.rs`) used relocate-keeping-module-names (drop `#[path]`, native resolution → zero ref churn); **entropy** was fully renamed (`entropy_fast`→`entropy::fast`, `+fast_x86/fast_neon/avx512`) joining `keywords`+`scanner` so all entropy code shares one home — refs rewritten via `::`-anchored sed (function names sharing the substring, e.g. `has_high_entropy_fast_x86`, untouched). Verification path that made the deferral safe: RTX 5090 host for `--features gpu --tests`, `--target aarch64-apple-darwin` cross-check for the `#[cfg(aarch64)]` neon impl, ci-lean for x86. Fixed 3 `include_str!` string literals the `::`-rewrite could not reach (2 entropy regression includes + the `gpu_shader_activation` test left stale by the gpu move). All four committed green; module maps in `lib.rs` updated to the new paths.
- 2026-06-13 · KH-L-0080 (follow-on: stale C10 FMA3 contract test re-anchored + dispatch comments made coherent) · 251bb5bb,7e443830,57379e2e · detection/test-truth(coherence) · Running (not just compiling) the relocated entropy tests surfaced a PRE-EXISTING failure: `regression_entropy_fma3_dispatch::avx2_reduction_declares_fma_target_feature` asserted the AVX2 reduction body contains `_mm256_fmadd_pd`, but that intrinsic was deliberately removed when both the AVX2 and AVX-512 reductions were unified onto the shared bit-exact `entropy::fast::entropy_from_histogram` (the old vectorized polynomial-log2 diverged ~5e-3 bits/byte and could flip an entropy gate near threshold). Re-anchored the test on the DURABLE C10 invariant — `shannon_entropy_avx2` is compiled `#[target_feature(enable="avx2,fma")]`, which licenses FMA3 emission and makes the gate's `fma` requirement load-bearing regardless of any explicit intrinsic — matching a real attribute line, not a doc-comment mention (the old `>=2` count passed only by counting a doc-comment string). NOT a weakening (Law 6/9): the SIGILL-safety contract stays fully asserted by the untouched `avx2_dispatch_gate_requires_fma_not_just_avx2`; restoring the intrinsic would REGRESS numerics. Also fixed the stale dispatch SAFETY comment in `entropy/fast.rs` (claimed the AVX-512 reduction calls `_mm512_cvtepi64_pd` — it now needs only `avx512f,avx512bw`; the gate's `avx512dq` is deliberate forward-headroom per `entropy/avx512.rs`, a sound over-gate). Entropy regression+standalone suites 36/36 green. Plus folded the last cross-crate prefix cluster: `core/src/allowlist_metadata.rs`→`allowlist/metadata.rs` (private submodule, zero external churn) — every crate src/ root is now free of `foo_*.rs` clusters. Task-#20 GPU-checkpoint audit outcome: `gpu/{gpu.rs,backend.rs}` are CLEAN — the `#[cfg(feature="gpu")]`/`not` pairs are the documented feature-gating, and every runtime GPU-MoE failure path is loud (`moe_runtime_degrade` one-shot stderr + `KEYHOG_REQUIRE_GPU` hard-fail), no silent fallback. The remaining `megakernel.rs` (573L) inline tests + split stay correctly DEFERRED behind KH-L-0402 ("once the architecture is final"; allowlisted today).
- 2026-06-13 · KH-L-0080 (follow-on: reorg broke 26+ runtime path-gate tests — found by RUNNING, fixed, full-suite reconciled) · 0f2e489e · org/test-truth(coherence/dogfood) · The module reorg compiled clean but the structural gate tests read source paths at RUNTIME (`std::fs::read_to_string(concat!(MANIFEST_DIR,"/src/<flat>.rs"))`), so `cargo check --tests` passed while 26+ panicked on the moved files — caught only by running the full `cargo test`. Repointed every reference at the new dir paths: the 24 per-module `unit::gates::*` checks, the `gap::*` source-readers, `tests/FILE_GATE_MATRIX.toml` (scanner AND core rows — both crates' `file_gate_matrix_*_missing_submodule_rows` walkers enumerate the real tree), `core/src/allowlist_metadata.rs`→`allowlist/metadata.rs`, plus coherence-only doc/divider/provenance refs and the `ml/*.py` source-of-truth port pointers. Lesson recorded to memory ([[module-move-breaks-runtime-path-gates]]): verify a move with the FULL scanner `all_tests` + `core` suite, not compile. **Reconciled full-suite state (post-fix, all PRE-EXISTING, NONE reorg-caused — do not chase as regressions):** scanner gate+gap = 331 pass / **3 fail** [`vyre_usage_matches_workspace_pin` (deliberate vyre path-override, no `vyre = "="` pin — clears when 0.6.2 ships); `detector_contract_coverage_100pct` (KH-GAP-011: detector ids lacking `tests/contracts/<id>.toml`); `findings_registry_integrity` (GAP_FINDINGS `test=` paths under gitignored `coordination/`)]. core = fully green. cli structural = 101 pass / **2 fail** [`gate::cli_surface_snapshot::scan_long_flag_set` (pinned scan-flag snapshot diverged); `integration::a8_harness::gate_dir_has_sixteen_tests` (hardcoded count asserts 16, dir now has 18 — brittle count, banned-hardcoded-list smell)]. These 5 are 5 distinct separate lanes; vyre is authorization-gated.
- 2026-06-13 · KH-L-0110 (CredData recall lane — root-caused the hex-key miss to a specific gate; quantified a mirror-safe wedge) · (analysis, no code yet) · detection-truth · **Empirical baseline** (release ci-lean v0.5.40, MoE `moe-v1-8c4d64ffb8fed94e`, on the real on-disk CredData corpus, 13918 labeled positives): overall TP **2406**, FP 1677, FN **11512**, recall **0.173**, precision 0.589, F1 0.267 — severely recall-bound, as the [[real-world-recall-gap-creddata]] memory predicted. **Where the recall bleeds** (`bench.creddata_miss_analysis shapes` — POS/NEG precision ceiling per shape×keyword): canonical-length hex WITH a credential keyword is a huge, clean bucket — hex48+kw **1033 POS / 0 NEG (P=1.0)**, hex32+kw 1279/31 (0.976), hex64+kw 1067/44 (0.96); the `crypto_key_hex` simulate candidate alone reaches 3363 TP @ 0.921 (more than keyhog's ENTIRE current TP). vs the FP-trap shapes (b64 ~0.38, uuid ~0.39, other ~0.18) that must NOT be blanket-surfaced. **Root cause** (dogfooded the release binary on a crafted probe: `password/token/secret/api_key = "<hex>"` ALL missed, while a mixed-case-alnum value caught via `entropy-token` @1.0, and a *lower*-entropy `password="hunter2…"` caught via `generic-secret` — so it is NOT an entropy floor; pure HEX is specifically excluded): the keyword bridge captures the value, then `suppression/decision.rs::should_suppress_inner` suppresses it via `!bypass_shape_gates && looks_like_bare_hex_digest` (shape_gates.rs: lengths 32|40|48|56|64|72|128). Generic-keyword pure-hex is reclassified to `weak_anchor`→`bypass_shape_gates=false` in `suppression/api.rs`, so the gate fires. **Mirror-safe wedge** (avoids the v31 catastrophe — lifting hex for `TOKEN=<hex>` added +5681 FP, P 0.97→0.33, because the mirror plants hex AS BOTH classes): the mirror `negatives.py` plants hex negatives ONLY at len 40 (sha1/git-sha) and 64 (sha256/docker-digest) + UUID — NEVER hex32 or **hex48**; and the decision.rs v18 FP regression (3304 FP) was ALL len 40/64. So **hex48 (and hex32) + a STRONG credential keyword** (key/secret/api_key/private_key/encryption_key/client_secret/access_key/signing_key/master_key — NOT bare token/id) is a +~2312 TP @ ~0.987 recall win that is invisible to both the mirror negatives and the v18 FP classes. **Knife-edge** (why 48 is in the digest set): it guards `[a-f0-9]{32,48}` NAMED-detector regexes (honeybadger) greedy-capturing the first 48 of a 64-hex sha256 = a TRUNCATED prefix. But that truncation only happens on the WEAK-ANCHOR NAMED path (`should_suppress_named_detector_finding_weak`); the keyword BRIDGE captures a COMPLETE delimited value (different entry point: `..._with_source_and_entropy` via `generic_value_shape_rejected`), so a bridge-captured 48-hex is provably the whole value, not a truncation. **Sound fix** = lift the bare-hex-digest gate for complete hex32/hex48 + strong keyword on the BRIDGE path ONLY, leave the weak-anchor named path (and hex40/hex64) untouched; **verify with BOTH bench gates** (CredData recall↑, mirror precision held) before landing — soundness over reach.
- 2026-06-13 · KH-L-0110 (SHIPPED — bridge-path canonical-hex-key exemption) · 5a16643f · detection-truth · Implemented the wedge exactly as scoped: a `allow_canonical_hex_key` flag threaded `fallback_generic.rs` (new `is_strong_keyword_anchored_hex_key`: complete pure-hex len 32|48 under a STRONG key keyword — secret/api_key/private_key/encryption_key/signing_key/access_key/client_secret/app_secret/master_key, NOT token/pass/auth) → `generic_value_shape_rejected` → `suppression::api`/`decision::should_suppress_inner`, skipping the bare-hex-digest + random-byte-blob + encoded-binary + base64-blob arms ONLY (prefixed-`sha256:`, UUID, repetitive, fake-sequence, placeholder all still fire). Soundness: GENERIC_RE group 2 is `{8,128}` so a len-48 bridge capture is the COMPLETE run, not the weak-anchor `{32,48}` truncated-sha256-prefix (that path untouched); 40/64 NOT exempted (the mirror's only hex hash-negatives + the v18 FP classes). **Verified, both real corpora + contracts:** CredData recall 0.1729→**0.1801**, precision 0.5893→**0.5998** (+101 TP, −4 FP, F1 0.2673→0.2770); SecretBench mirror precision **0.9954 ≥ 0.9945 floor** (recall 0.8433→0.8607, FP 14→12) — no precision regression, opposite of the v31 catastrophe; `contracts_runner` 6/0 (no detector contract moved); `regression_creddata_hex_key_recall` (NEW) pins the lift + every guard via the shipped binary; dogfood probe 10/10. Headroom remains (the shape ceiling is ~2312 keyword-anchored hex32/48 POS; +101 is the increment after existing named-detector/entropy coverage and the deliberately-tight strong-keyword set) — next: diagnose the still-missed hex keys' keywords/formats before any broadening, always re-gating on the mirror.
- 2026-06-13 · KH-L-0110 (LANE DIAGNOSED — the remaining hex-key "headroom" is a CredData artifact; declining to chase it) · (analysis + tooling) · detection-truth/test-depth · New `bench.creddata_miss_analysis keywords` mode buckets the mirror-safe lengths (hex32/hex48 — the mirror plants **0** negatives there, only hex40/hex64; verified directly against `corpora/mirror/manifest.jsonl`: 1868 hex40-NEG + 1512 hex64-NEG, **0** hex32/48-NEG) by CANONICAL keyword on the real corpus. Result: the ~2312-POS ceiling is **2266 POS concentrated under a single canonical keyword `key`** (P=0.987) — and of those, **2264/2266 live in ONE repo-prefix `data/2ba83c6a`, 1910 in ONE file** (`test/911dde03.txt`), token literally `Key` (2295×) vs `key` (1×), all `Key = <hex>` in `test/` paths. The 30 CredData NEG under `key` are **all repetitive-run decoys** (`ECECEC…`, `DEDEDE…`) the exemption's preserved repetitive arm already kills. **Why I am NOT lifting bare `key`** (soundness/Screwdriver over a benchmark number): (1) GENERALIZATION — +1910 of the gain is one fixture file, not a real-world distribution; the uniform `Key`-capitalization confirms a single generator/source. (2) Law 7 perf — routing it requires adding bare `key` to the `GENERIC_ASSIGNMENT_KEYWORDS` AC prefilter, but `key` is a substring of `keyword`/`monkey`/`keyboard`/`turkey`/… so the expensive `GENERIC_RE` (~500µs/chunk) would fire on a large fraction of all lines. (3) test-path penalty would haircut them anyway. (4) the mirror is *blind* here (0 hex32/48 negs), so it cannot guard a bare-`key` broadening — the only evidence is CredData, and CredData says "one file." The genuinely-distributed keyworded hex32/48 POS (sharedsecret/apikey/hexsalt/secret/token ≈114 across many keywords) are already covered by the shipped strong set + named detectors. **Conclusion: the bridge-path hex-key lane is saturated for sound gains; the large remaining CredData pools are b64 (P≈0.38) / uuid (P≈0.39) — low-precision shapes that need ML/context, not a shape+keyword lift** ([[ml-model-must-train-on-real-distribution]]). Proving test: `benchmarks/bench/tests/test_creddata_miss_analysis.py` (2/2, asserts the per-keyword bucketing/precision/length-filter on a synthetic corpus).
- 2026-06-13 · KH-L-0110 (CredData recall CEILING characterized — where the real headroom is, by POS file/repo concentration) · (analysis) · detection-truth · Measured per-shape POS concentration to separate genuine distributed recall headroom from single-fixture artifacts: **b64 5178 POS across 276 repos / 1860 files** (top repo 13%, top file 96) and **uuid 2435 POS across 95 repos / 523 files** (top repo 20%) are GENUINELY DISTRIBUTED — real headroom — but both sit at shape-precision ≈0.38–0.39 (a blanket shape+keyword lift would add ~62% FP, the v31 trap), so they need ML/context discrimination, NOT a shape rule. By contrast **hex64 (1270 POS, 89% in `data/2ba83c6a`, 955 in one file) and hexother (844 POS, 82% same repo, 325 in one file) are the SAME synthetic fixture artifact** as the hex32/48 `key` pool — confirming the decision to exempt only hex32/48 under strong keywords and to NOT chase hex64/40/other. **Strategy:** the next real recall lever is the b64/uuid pools via an MoE retrain on the real distribution (surface the candidates + let a real-distribution-trained scorer separate the ~38% real from ~62% decoy) — the heavy [[ml-model-must-train-on-real-distribution]] loop, not heuristics. Shape-rule recall on CredData is now saturated.
- 2026-06-13 · KH-L-0080 (78046450 GPU-checkpoint structural debt — 3 RED gate lanes reconciled) · 8c248280, d22c4774, 4952089f · test-depth/org/architecture · Three pre-existing CI failures left by the in-flight megakernel checkpoint, all fixed soundly (no test weakened): **(1) gate-count tripwires** — cli `tests/gate` had drifted to 18 files vs a hardcoded `assert_eq!(n, 16)`; replaced all four crates' brittle magic-constant gate-count tests with a data-driven WIRING-coherence check (every `tests/gate/*.rs` is declared `mod` in mod.rs — catches dead/unwired gate files, strictly stronger) + a retention FLOOR (per-crate migration baseline 6/8/9/16) that never breaks on a legitimate addition (GENERALIZATION + INTROSPECTION: one shared cause, four sites). **(2) cli_surface_snapshot** — `--no-config` (unconditional, MC-07) was added to ScanArgs without updating the CLI-08 pin; added to `BASE_SCAN_LONG_FLAGS` (the deliberate audit-trail update the gate forces). **(3) findings_registry_integrity** — the checkpoint deleted the megakernel SPEC-waiver subsystem but left findings 043/103/098 pointing at the 3 deleted files. Verified the architectural truth still holds (no `engine/*.rs` reads `KEYHOG_USE_MEGAKERNEL`; megakernel proven on-demand by `megakernel_cpu_parity.rs` #[cfg(gpu)] --ignored, not the live backend) and RESTORED the subsystem: recreated the waiver `.toml` + `support/megakernel_waiver.rs` predicates + the `docs_megakernel_env_claim_matches_engine` coherence gate, and repointed KH-GAP-043's test from the obsolete env-toggle `megakernel_literal_set_parity.rs` (the env is now a no-op → that test would be meaningless) to the real `megakernel_cpu_parity.rs`. Proving tests (all green): `gate_dir_files_are_wired_and_meet_floor` ×4, `scan_long_flag_set_matches_pinned_snapshot`, `findings_registry_integrity`, `waived_findings_registry_has_expiry_fields`, `gap_findings_has_eighty_entries`, `gap_findings_ids_unique`, `docs_megakernel_env_claim_matches_engine`. Remaining pre-existing REDs in this cluster (separate lanes): `vyre_usage_matches_workspace_pin` (blocked on the uncommitted vyre-0.6.2 path-override — authorization-gated), `detector_contract_coverage` (KH-GAP-011).
- 2026-06-13 · KH-L-0080 (4th RED lane — detector contract coverage 100%) · fd093a1d · test-depth · `detector_contract_coverage_100pct::every_detector_id_has_contract` requires every loaded detector id to ship `tests/contracts/<id>.toml`; exactly ONE detector (`basic-auth-credentials`) had none (computed authoritatively: 902 ids, only that stem missing; the 4 filename "orphans" all carry a resolving `detector_id` so they don't fail). Authored the full per-rule contract (3 positives — RFC 7617 header / curl / hardcoded source; 4 negatives — `Basic auth`, `basics`, `Basic Plan`, sub-16-char blob; 2 evasions — lower-cased scheme, commented header; perf+scale budgets), base64 blobs verified as real `base64(user:pass)`. Verified EMPIRICALLY against the full pipeline (not just regex): `contracts_runner` 6/0 (positives surface, negatives don't fire under the detector, evasions survive) + `every_detector_id_has_contract` green. **Structural-debt cluster status: 4 of 5 pre-existing REDs now green** (gate-count ×4 crates, cli surface snapshot, findings_registry subsystem, detector contract coverage); only `vyre_usage_matches_workspace_pin` remains, blocked on the authorization-gated vyre-0.6.2 path-override.
- 2026-06-13 · KH-L-0080 (5th RED lane — vyre_usage pin parser; NOT authorization-gated as the summary claimed) · 64488918 · test-depth/coherence · Read the code instead of trusting the prior-session summary (Law 11): `vyre_usage_doc_matches_workspace_pin` panicked at `.expect("root Cargo.toml must pin vyre")` because `workspace_vyre_pin` only parsed the bare-string form `vyre = "=0.6.1"`, but the COMMITTED root Cargo.toml (clean, not dirty — the path override is committed, contradicting the stale memory that called it uncommitted) uses the inline-table form `vyre = { version = "=0.6.1", path = "..." }`. So the parser returned None and panicked — a stale-parser bug, NOT a path-override/authorization issue: the pin (0.6.1) and the doc (`vyre v0.6.1`) are already coherent. Fixed the parser to extract `"=<version>"` from either layout; Cargo.toml untouched. Green locally and in a clean checkout (the parser reads manifest text, independent of whether the path dep resolves). **Structural-debt cluster: ALL 5 pre-existing REDs from checkpoint 78046450 now green.** Genuinely-deferred follow-on (separate hard lane, KH-L-0402 / task #10): wiring the megakernel as the LIVE GPU backend once vyre exposes a stable FileBatch hook — the gap the KH-GAP-043 SPEC waiver (expires 2026-08-01) covers.
- 2026-06-13 · KH-L-0402 (megakernel.rs Law-5 split — the checkpoint's named "megakernel split" item) · bb738a1f · architecture · `engine/megakernel.rs` was 573 LOC (>500 cap) carrying two responsibilities: catalog build+GPU-dispatch and the on-disk cache wire (de)serialization. Extracted the wire domain (`CatalogWireError`, `WireReader`, `MatchEngineCache for MegakernelCatalog` to_bytes/from_bytes + round-trip test) to a sibling `engine/megakernel_wire.rs` (573→418 LOC; wire ≈190). Sibling layout matches engine/'s flat convention (cf. `megakernel_dispatch.rs`); `MegakernelCatalog` fields + `CATALOG_WIRE_MAGIC` made `pub(super)` (engine-scoped, no public-surface widening) so the sibling serializes/reconstructs without a `megakernel/` subdir. Wire format is a stable boundary independent of build/dispatch, so it survives the pending live-wiring (Law 1). Verified under `--features gpu` (uses the local vyre path-override for the megakernel symbols): scanner lib compiles; `catalog_classifies_lowerable_vs_host` + `catalog_empty_when_nothing_lowers` (in megakernel) + `catalog_wire_roundtrip` (moved to megakernel_wire) all green 3/0. Remaining engine >500-LOC files (separate Law-5 lanes, CPU-path so ci-lean-verifiable): `fallback_compiled.rs` 530, `fallback_prefilter.rs` 535.
- 2026-06-13 · KH-L-0402 (inline-test gate follow-up to the split) · c4408584 · test-depth · The split moved `catalog_wire_roundtrip` into `engine/megakernel_wire.rs`, which tripped `no_inline_tests_in_src` (the new src file has `#[cfg(test)]` but wasn't allowlisted) — caught by re-running the inline-test gates after the split, not assumed green. The test asserts on the catalog's private fields + calls `pub(crate)`/`MatchEngineCache`, so an external `tests/` file would force exposing GPU internals `pub` (the trade the allowlist exists to refuse, Law 1). Added `engine/megakernel_wire.rs` to `INLINE_TEST_ALLOWLIST` with that justification and updated the `megakernel.rs` entry (the wire test left it; it now holds only catalog-classification tests). The stale-allowlist guard confirms both entries still hold real inline tests; gate green. **Task #20 (78046450 GPU-checkpoint structural debt: stale gates + inline tests + megakernel split) — all three items resolved.** This was an introduced regression from my own split, found + fixed in-session (the split commit bb738a1f alone is momentarily RED on this one gate; follow-up c4408584 closes it — both unpushed, tree green together).
