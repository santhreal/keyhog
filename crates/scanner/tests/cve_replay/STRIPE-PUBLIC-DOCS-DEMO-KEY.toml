# Stripe's public-docs demo secret key. Published in Stripe's testing
# documentation as a sample value for `STRIPE_SECRET_KEY`. The key is
# not live (Stripe revoked it long ago), but the SHAPE is exact —
# `sk_live_` prefix + 24+ alphanumeric — so any future regex change
# that drops Stripe live keys will fail this contract.

schema_version = 1
cve_id = "STRIPE-PUBLIC-DOCS-DEMO-KEY"
source_url = "https://stripe.com/docs/keys"
detectors = ["stripe-secret-key"]
service = "stripe"
description = "Stripe demo sk_live_ key from public testing documentation."
leaked_text = """
# Stripe documentation sample (revoked).
STRIPE_SECRET_KEY=sk_live_4eC39HqLyjWDarjtT1zdp7dc
stripe.api_key = "sk_live_4eC39HqLyjWDarjtT1zdp7dc"
"""
