schema_version = 1
detector_id = "upstash-kafka-credentials"
service = "upstash"
severity = "critical"

# Auto-generated contract (gen_contracts.py). Hand-edit to add more
# real-world shapes; the auto-stub validates the detector at
# least fires on its canonical-shape anchor + body.

[[positive]]
text = "https://afmi92zj0y0jmsj-kafka.upstash.io"
credential = "https://afmi92zj0y0jmsj-kafka.upstash.io"
reason = "Canonical anchor + synthesized body satisfying detector's primary regex."

[[positive]]
text = "https://afmi92zj0y0jmsj-kafka.upstash.io\""
credential = "https://afmi92zj0y0jmsj-kafka.upstash.io"
reason = "Quoted-value variant of the canonical positive."

[[negative]]
text = "YOUR_API_KEY_HERE_PLACEHOLDER_VALUE"
reason = "Placeholder-keyword body (suppression gate matches PLACEHOLDER prefix)."

[[negative]]
text = "httpsEXAMPLEEXAMPLEsh.io"
reason = "EXAMPLE token marker inside the body (suppression gate strips it)."

# ---- EVASION (adversarial) -----------------------------------------

[[evasion]]
text = "# UPSTASH_KAFKA_REST_URL=https://afmi92zj0y0jmsj-kafka.upstash.io  # queue"
credential = "https://afmi92zj0y0jmsj-kafka.upstash.io"
reason = "Commented-out config; the ...-kafka.upstash.io endpoint URL is self-anchoring and surfaces from the comment."
