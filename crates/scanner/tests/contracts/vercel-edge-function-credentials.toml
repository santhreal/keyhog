schema_version = 1
detector_id = "vercel-edge-function-credentials"
service = "vercel"
severity = "high"

# Auto-generated contract (gen_contracts.py). Hand-edit to add more
# real-world shapes; the auto-stub validates the detector at
# least fires on its canonical-shape anchor + body.

[[positive]]
text = "ecfg_VWTr5j5Y"
credential = "ecfg_VWTr5j5Y"
reason = "Canonical anchor + synthesized body satisfying detector's primary regex."

[[positive]]
text = "ecfg_VWTr5j5Y\""
credential = "ecfg_VWTr5j5Y"
reason = "Quoted-value variant of the canonical positive."

[[negative]]
text = "YOUR_API_KEY_HERE_PLACEHOLDER_VALUE"
reason = "Placeholder-keyword body — suppression gate matches PLACEHOLDER prefix."

[[negative]]
text = "ecfg_EXAMPLEEXAMPLEr5j5Y"
reason = "EXAMPLE token marker inside the body — suppression gate strips it."

[[negative]]
text = "CUDNN_BACKEND_ENGINECFG_DESCRIPTOR"
reason = "cuDNN SCREAMING_CASE enum: 'ENGINECFG_' contains 'ecfg_' mid-identifier when matched case-insensitively. The \\b word boundary on the ecfg_ prefix must exclude it (regression: 3 FPs on /usr/include/x86_64-linux-gnu/cudnn_backend.h)."

# ---- EVASION (adversarial) -----------------------------------------

[[evasion]]
text = "# edge fn (rotate): ecfg_VWTr5j5Y"
credential = "ecfg_VWTr5j5Y"
reason = "Commented-out token; the ecfg_ prefix anchor surfaces it (shares the ecfg_ shape with the sibling vercel-edge-config-token — same prefix family)."
