# SPEC waiver — KH-GAP-043 / megakernel GPU≡CPU parity
# Megakernel dispatch is intentionally unwired in engine/ until vyre exposes a
# stable FileBatch hook. The parity contract is enforced on demand by
# crates/scanner/tests/megakernel_cpu_parity.rs (#[cfg(feature = "gpu")], run
# with --ignored on a GPU host); it is not yet a live default-backend CI gate,
# which is what this waiver covers.
gap_id = "KH-GAP-043"
kind = "spec"
reason = "MegakernelScanner dispatch is not wired in engine/ as the live backend until vyre exposes a stable FileBatch hook; KEYHOG_USE_MEGAKERNEL must remain a no-op (the engine reads no such env) until the parity contract (tests/megakernel_cpu_parity.rs) is promoted to a continuous gate."
expires = "2026-08-01"
owner = "scanner-engine"
fix_target = "0.6.0"
