# Dogfood release-gate change log
#
# One row per intentional change to a scenario's expected output. A diff in
# the gate without a row here is an unexplained regression and blocks release.
#
# date        scenario                  why
2026-05-28    (initial)                 Seed gate: aws-env, clean, fp-trap, multi-detector, json-envelope, sarif, detectors-list.
