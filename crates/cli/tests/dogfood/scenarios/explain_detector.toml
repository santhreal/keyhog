description = "explain renders a detector's spec: name, severity, and regex pattern, exit 0"
args = ["explain", "aws-access-key"]
exit_code = 0
max_ms = 20000
stdout_contains = ["aws-access-key", "AWS Access Key", "critical", "(AKIA|ASIA)"]
