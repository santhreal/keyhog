description = "Human text format renders the detector name and a result summary, exit 1"
corpus = [".env.aws-leak"]
args = ["scan", "--format", "text", "{corpus}/.env.aws-leak"]
exit_code = 1
max_ms = 20000
stdout_contains = ["AWS Access Key", "1 secret found"]
