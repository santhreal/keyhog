description = "SARIF output is well-formed v2.1.0 and carries the finding rule id"
corpus = [".env.aws-leak"]
args = ["scan", "--format", "sarif", "{corpus}/.env.aws-leak"]
exit_code = 1
max_ms = 20000
stdout_contains = ["\"$schema\"", "2.1.0", "aws-access-key"]
