# Synthetic deployment env used only by the dogfood release-gate.
# The AWS key below is a fixed, well-formed but non-live sample so the
# scenario asserts a stable line + detector. Never a real credential.
DB_HOST=prod-db.internal.example.com
DB_PORT=5432
AWS_ACCESS_KEY_ID=AKIAQYLPMN5HFIQR7XYA
AWS_SECRET_ACCESS_KEY=wJalrXUtnFEMI/K7MDENG/bPxRfiCYEXAMPLEKEY
LOG_LEVEL=info
