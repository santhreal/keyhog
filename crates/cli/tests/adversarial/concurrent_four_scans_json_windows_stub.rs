//! Windows oracle: concurrent tempdir scans (shared hostile coverage on all targets).

#[test]
fn concurrent_four_scans_json_windows_stub() {
    crate::support::oracle_concurrent_four_scans_json();
}
